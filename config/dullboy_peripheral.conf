CONFIG_ZMK_BLE=y
CONFIG_BT_PERIPHERAL=y

# 2 dongles + 2 hôtes BLE directs
CONFIG_ZMK_BLE_PROFILE_COUNT=4

# Une seule connexion active à la fois (recommandé dans ce scénario)
CONFIG_BT_MAX_CONN=1